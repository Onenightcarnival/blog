---
date: 2026-04-11 16:00
---

模型输出不稳定，加规则。新规则跟旧规则冲突，加更多规则说明优先级。Prompt 从 200 token 膨胀到 2000 token，每改一个词都可能引发蝴蝶效应。最后没人敢动，因为没人知道每条规则为什么在那里。

2000 token 的 prompt 就是碰不得的遗留代码。而这个循环的根源不是 prompt 写得不好——是方向就错了：你在 prompt 层面追求确定性，但 prompt 给不了你确定性。
